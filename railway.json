{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/switch-db.mjs postgres && npx prisma generate && npm run build:next"
  },
  "deploy": {
    "preDeployCommand": "node scripts/switch-db.mjs postgres && npx prisma db push --skip-generate",
    "startCommand": "npm run start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
